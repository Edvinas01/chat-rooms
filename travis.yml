language: java
jdk:
  - oraclejdk8
before_install:
  - cd server
  - chmod +x gradlew
script:
  - cd server
  - ./gradlew check
cache:
  directories:
  - ~/.gradle
after_success:
  - ./gradlew cobertura coveralls
notifications:
  email:
    on_success: change